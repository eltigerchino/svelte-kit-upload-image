{
	"name": "sveltekit-image-upload-updated",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "vite build && mkdir build/public",
		"start": "BODY_SIZE_LIMIT=0 ORIGIN=http://localhost:3000 node custom-server",
		"check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-node": "^1.2.4",
		"@sveltejs/kit": "^1.20.4",
		"svelte": "^3.59.2",
		"svelte-check": "^3.4.3",
		"typescript": "^5.1.3",
		"vite": "^4.3.9"
	},
	"type": "module",
	"dependencies": {
		"polka": "^0.5.2",
		"serve-static": "^1.15.0"
	}
}
